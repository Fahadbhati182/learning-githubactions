name: My first workflow
on: [push, workflow_dispatch]
jobs:
  my-first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Pull File 
        uses: actions/checkout@v7
      - name: List all file 
        uses: ls -la
